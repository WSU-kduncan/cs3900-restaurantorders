export class User 
{
    //NOTE: the class notes say this:
    //generate the models
        //ng generate model models/user
        //ng generate model models/product
    //this doesnt.... work. youre supposed to do ng generate class user --type=model instead. possibly something to fix.

}
